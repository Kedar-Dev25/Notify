name: Notify Cron Job

on:
  schedule:
    - cron: "*/5 * * * *"

  workflow_dispatch:

jobs:
  call-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger API
        run: |
          curl -X GET "https://YOUR-BACKEND-URL/api/send-notification"
